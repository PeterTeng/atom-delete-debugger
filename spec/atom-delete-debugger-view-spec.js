'use babel';

import AtomDeleteDebuggerView from '../lib/atom-delete-debugger-view';

describe('AtomDeleteDebuggerView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
